# elisee.dagbo.m1ecap

Package d'analyse de la diversite des poissons dans la Seine.

## Source des donnees

Jeu de donnees open data :
https://explore.data.gouv.fr/fr/datasets/57868eb7a3a7295d371add08/#/resources/e86064d6-0b0b-4e1b-90fb-fe016fd8e117

## Git

Le premier commit de ce repertoire a ete effectue le 16/04/2026 a 09h33.

### Exercice Git

Completer la suite de commande `git` a executer (hors `git add`) pour obtenir l'historique demande :

```bash
git checkout master
git commit -m "commit A"
git checkout -b branche-1
git commit -m "commit B"
git checkout master
git checkout -b branche-2
git commit -m "commit C"
git checkout master
git merge branche-1
git merge branche-2
